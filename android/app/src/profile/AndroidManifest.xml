<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Lets the Flutter tool talk to the running app over the network. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
